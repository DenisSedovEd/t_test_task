# 📝 Мини-CRM: Сервис распределения лидов

Проект реализует асинхронный сервис на **FastAPI** и **SQLAlchemy** для автоматического распределения входящих обращений (контактов) между операторами с учётом их нагрузки и заданных весовых коэффициентов по источникам.

## 🚀 1. Как запустить проект

Для запуска необходим только установленный **Docker Engine**. Используется многостадийная сборка для оптимизации размера образа.

1.  **Сборка образа:**
    ```bash
    docker build -t lead-distribution-crm .
    ```

2.  **Запуск контейнера:**
    ```bash
    docker run -d --name crm_service -p 8000:8000 lead-distribution-crm
    ```
    *(Сервис будет доступен по адресу: http://localhost:8000/)*

3.  **Документация API:**
    * Просмотреть все доступные эндпоинты и протестировать логику можно в **Swagger UI**: **http://localhost:8000/docs**

---

## 🏗️ 2. Модель данных (Сущности и Связи)

Архитектура включает пять ключевых сущностей:

| Сущность | Назначение | Ключевые параметры |
| :--- | :--- | :--- |
| **Operator** | Сотрудник. Может получать новые обращения. | `is_active` (активен), `max_load_limit` (максимальный лимит активных обращений). |
| **Source** | Канал обращения (например, бот). | `name`. |
| **Lead** | Конечный клиент. | **`external_id`** (уникальный внешний идентификатор). |
| **Contact** | Факт обращения. | `status` (статус обращения, где `active` учитывается в нагрузке). |
| **SourceOperatorWeight** | Связующая таблица для настройки весов. | `weight` (числовой вес/компетенция оператора для конкретного источника). |

**Связи:**
* **Lead (1) : Contact (М)**
* **Source (1) : Contact (М)**
* **Operator (1) : Contact (М)**
* **Operator (М) : Source (М)** (через `SourceOperatorWeight`)

---

## ⚙️ 3. Алгоритм распределения обращений

Логика распределения запускается при регистрации нового обращения через `POST /api/v1/contacts/`.

### А. Определение принадлежности к Лиду
При поступлении обращения, лид однозначно определяется по **`lead_external_id`**. Система ищет существующего лида по этому ID или создаёт нового, если совпадение не найдено.

### Б. Учет лимитов и активности (Фильтрация)
Нагрузка оператора рассчитывается как **общее количество контактов со статусом 'active'**, закрепленных за ним. Из пула кандидатов исключаются (фильтруются):
* Операторы, у которых `is_active = False`.
* Операторы, чья текущая нагрузка равна или превышает `max_load_limit`.

### В. Распределение по весам (Случайный выбор)
Распределение производится среди **доступных** (отфильтрованных) операторов методом **случайного выбора с вероятностью** (`random.choices`). Доля обращений в среднем соответствует заданным весам.

### Г. Действия при отсутствии операторов
Если после всех фильтров список доступных операторов пуст, обращение **создается в базе данных**, но поле **`operator_id`** устанавливается в **`NULL`**. Это позволяет сохранить данные об обращении для дальнейшей ручной обработки.